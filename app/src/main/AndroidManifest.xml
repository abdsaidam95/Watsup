<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chatwhatsapp">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".feature.activity.chat.ChatActivity"
            android:parentActivityName=".feature.MainActivity"></activity>
        <activity android:name=".feature.activity.profile.ProfileActivity" />
        <activity
            android:name=".feature.activity.findFriends.FindFriendsActivity"
            android:parentActivityName=".feature.MainActivity" />
        <activity android:name=".feature.activity.phoneLogin.PhoneLoginActivity" />
        <activity android:name=".feature.activity.group.GroupChatActivity" />
        <activity android:name=".feature.activity.setting.SettingsActivity"
            android:parentActivityName=".feature.MainActivity">

        </activity>
        <activity android:name=".feature.activity.register.RegisterActivity" />
        <activity android:name=".feature.activity.login.LoginActivity" />
        <activity android:name=".feature.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
    </application>

</manifest>